<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Nutrician.HomePage"
             BackgroundColor="LightBlue">
    <ContentPage.Content>
        <StackLayout>
            <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Row="0" Grid.Column="0" Text="Home" Padding="5" ></Label>
                    <Label Grid.Row="0" Grid.Column="1" Text="Nutrician" Padding="5" ></Label>
                    <Label Grid.Row="0" Grid.Column="2" Text="Welcome xxxx!" Padding="5"></Label>
                </Grid>

            </StackLayout>
            <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand">
                <StackLayout Orientation="Vertical" HorizontalOptions="FillAndExpand">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>
                        <Button
                        Grid.Row="1"
                        Margin="10"
                        VerticalOptions="Start"
                        HorizontalOptions="Start"
                        Grid.Column="0"
                        Text="MyList"
                        TextColor="LightPink"
                        BackgroundColor="LightGreen"
                        BorderColor="LightGreen"
                        BorderWidth="1.5"
                        CornerRadius="50"
                        Clicked="Button_Clicked">
                        </Button>
                        <Button
                        Grid.Row="1"
                        Margin="80"
                        HorizontalOptions="End"
                        Grid.Column="1"
                        Text="Reminders"
                        TextColor="DarkMagenta"
                        BackgroundColor="ForestGreen"
                        BorderColor="ForestGreen"
                        BorderWidth="1.5"
                        CornerRadius="50"
                        Clicked="Button_Clicked2">
                        </Button>
                    </Grid>
                </StackLayout>
            </StackLayout>
            <Entry Placeholder="Search!..." VerticalOptions="Center"/>
            <StackLayout>
                <CollectionView x:Name="collectionView">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>